# Development and contribution dependencies
# Install with: pip install -r requirements-dev.txt
-r requirements.txt

# Notebook metadata stripping so not committing to project to remove possible merge conflicts in notebooks
nbstripout>=0.6.0

# Code quality and formatting
black>=23.0.0
flake8>=6.0.0

# Testing
pytest>=7.4.0
